# من المعادلة إلى الكود — خريطة الربط

هذا الملف يربط كل معادلة فيزيائية من تقرير الدراسة ("النمذجة الرياضية والمحاكاة الحاسوبية لحركة كرة الغولف") بمكانها الدقيق في الكود المصدري. الهدف إثبات أن كل سطر برمجي مشتق مباشرة من معادلة موثّقة في التقرير، وليس كودًا عشوائيًا.

## 1. الثوابت الفيزيائية (القسم 2، Eq.1)

| الرمز بالتقرير | القيمة | مكان التعريف |
|---|---|---|
| m (كتلة الكرة) | 0.04593 kg | `src/constants.js → MASS` |
| r (نصف القطر) | 0.02133 m | `src/constants.js → RADIUS` |
| A (المساحة المقطعية، Eq.1) | 0.00143 m² | `src/constants.js → AREA` |
| ρ (كثافة الهواء) | 1.225 kg/m³ | `src/constants.js → AIR_DENSITY` |
| CD (ثابت السحب) | 0.22 | `src/constants.js → CD` |
| g (الجاذبية) | 9.806 m/s² | `src/constants.js → GRAVITY` |
| S (معامل ماغنوس، Eq.8) | 5×10⁻⁶ kg·m | `src/constants.js → MAGNUS_S` |
| Δt (خطوة التكامل، القسم 8.1) | 0.01 s | `src/constants.js → FIXED_DT` |

## 2. ديناميكا الانطلاق (القسم 3، Eq.2–4)

تحليل السرعة الابتدائية إلى مركبتين + متجه الدوران → `src/physics/BallState.js → init()`
(انظر التعليق الداخلي فيها لشرح تدوير محور الدوران مع زاوية التصويب aimDeg)

## 3. تحليل القوى الهوائية أثناء الطيران (القسم 4)

| القسم | المعادلة | الدالة |
|---|---|---|
| 4.1 الجاذبية | Eq.5 — F⃗g = m·g⃗ | `src/physics/Forces.js → gravity()` |
| 4.2 مقاومة الهواء | Eq.6 — F⃗d = -½·CD·ρ·A·\|v⃗rel\|·v⃗rel | `src/physics/Forces.js → drag()` |
| 4.3 الرفع وماغنوس | Eq.7 — F⃗M = S·(ω⃗×v⃗rel) | `src/physics/Forces.js → magnus()` |
| 4.4 الرياح والسرعة النسبية | Eq.9 — v⃗rel = v⃗ball − v⃗wind | داخل `drag()` و`magnus()` — كلاهما يحسب vrel محليًا |

المجموع الشعاعي (Eq.10) في `src/physics/Forces.js → totalForce()`، وهي الدالة الوحيدة التي يستدعيها المكامل العددي.

## 4. جملة المعادلات التفاضلية (القسم 5، Eq.11–13)

`totalForce()` تُعيد القوة الكلية على المحاور الثلاثة؛ التكامل العددي في `src/physics/PhysicsEngine.js → step()`:
- طريقة شبه ضمنية لأويلر (Semi-implicit Euler، القسم 8.1) — السرعة تُحدَّث أولًا ثم الموقع
- Δt = 0.01 s ثابتة، مطابقة لما ورد بالقسم 8.1

## 5. فيزياء الارتداد والدحرجة (القسم 6)

| القسم | المعادلة | مكان التنفيذ |
|---|---|---|
| 6.1 معامل الارتداد المتغير | Eq.14–15 (Penner p.933) | `src/physics/CollisionHandler.js → _bounce()` (RESTITUTION_A/B/C) |
| 6.2/6.3 الانزلاق مقابل الدحرجة | Eq.16–18 | `_bounce()` — فحص معامل الاحتكاك الحرج `mu_c` |
| 6.4 شرط الانتقال للدحرجة | عتبة الارتفاع (Penner p.935) | `_bounce()` → `CONSTANTS.ROLL_THRESHOLD` |

## 6. المستويات المائلة (القسم 7، Eq.19–22 — Rod Cross 2024)

`src/physics/CollisionHandler.js → _roll()` — بدل استخدام صيغة الميل الثابت فقط، الكود يحسب متجه الميل الفعلي `dhdx, dhdz` من التضاريس الحقيقية عبر `terrain.getHeightAt()` (تفاضل عددي مركزي)، ثم يطبّق:
- التباطؤ الاحتكاكي: μ·g/|n| (مكافئ لـ μ·g·cosθ في Eq.21)
- مركبة الجاذبية على المنحدر: −g·dhdx/|n|² و−g·dhdz/|n|² (مكافئ لـ Eq.19–20 و Eq.22)

هذا التعميم يجعل المعادلة تعمل مع أي تضاريس ثلاثية الأبعاد (مولّدة أو ماسح حقيقي)، وليس فقط منحدر بزاوية ثابتة.

## 7. التحقق الرقمي (القسم 9)

جدول التتبع اليدوي بالتقرير (v₀=50 m/s، θ=15°) قابل لإعادة إنتاجه مباشرة عبر إطلاق ضربة بنفس المعاملات ومراقبة `Flight Statistics` (Ball Speed / Altitude / Sim Time) في كل خطوة من 0.01s — القيم تتطابق مع نفس الاتجاه التناقصي/التزايدي الموصوف في التقرير (تسارع أفقي متناقص تدريجيًا، ارتفاع متزايد بمعدل غير خطي).

## 8. ما هو إضافة هندسية على الدراسة الأصلية (وليس من التقرير)

هذه العناصر أُضيفت لأغراض العرض والتفاعل، وليست جزءًا من المعادلات الفيزيائية نفسها:
- نظام تحديد الحفرة/الخريطة (`CourseMesh.js → loadHole()`)
- استيراد تضاريس حقيقية من مسح درون (`CourseMesh.js → loadScannedTerrain()`) — مصدر الارتفاع الحقيقي يُستبدل مكان `_rawHeight()` المولّدة رياضيًا، لكن كل معادلات الحركة (Eq.5–22) تبقى كما هي دون أي تغيير
- الشخصية الثابتة والكرة ذات النقر (تجميلي بصري بحت، صفر تأثير على الفيزياء)
- لوحة الإحصائيات وسجل الضربات (عرض بيانات فقط)
